# GUARD ts-navigation-refresh-highlight
#
# A background title/CWD refresh used to reset keyboard navigation to row zero.
# The break restores that reset after snapshot commit; the active row must
# survive refresh and clamp to the final row if the catalog page shrinks.
test: navigation retains keyboard highlight through a metadata snapshot refresh
red: 2026-09-07 at 962dfc1
build: test -Dphux-enabled=true
diff --git a/src/core.ts b/src/core.ts
index 48a5d967..7d44efc7 100644
--- a/src/core.ts
+++ b/src/core.ts
@@ -280,7 +280,7 @@ function closePalette(model: Model): Model {
 
 function refreshNavigation(model: Model): Model {
   const refreshed = requestNavigation(model, model.paletteOffset);
-  return { ...refreshed, paletteCursor: model.paletteCursor };
+  return refreshed;
 }
 
 function validNavigationPick(model: Model, index: number): boolean {
