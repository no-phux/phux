# GUARD remote-configured-colors
# Restore 72db6128's default-only copyClient policy. The test drives canonical
# frames through provider/Host publication and then asserts configured colors
# after OSC110/111/112 reset, not a direct copyWithMetadata call.
test: shipping Host preserves configured colors across OSC reset and idle theme repaint
red: 2026-09-07 at e54d49a
build: test -Dplatform=null -Dphux-enabled=true -Dphux-client-ffi-profile=ffi-dev
diff --git a/src/providers/phux/presentation.zig b/src/providers/phux/presentation.zig
--- a/src/providers/phux/presentation.zig
+++ b/src/providers/phux/presentation.zig
@@ -61,1 +61,1 @@
-        return store.copyWithMetadata(gpa, view, &metadata, store.policy);
+        return store.copyWithMetadata(gpa, view, &metadata, .{});
