# GUARD ts-remote-background-pointer
# App activation is not permission for wheel/drop input on macOS. Restore the
# overbroad focus gate; an inactive window must stop accepting both operations.
test: shipping background windows accept wheel scrolling and Finder drops
red: 2026-09-07 at 5ea82c2
diff --git a/src/cockpit/native/ts_engine.zig b/src/cockpit/native/ts_engine.zig
--- a/src/cockpit/native/ts_engine.zig
+++ b/src/cockpit/native/ts_engine.zig
@@ -1351,1 +1351,1 @@
-        return !self.input_suspended;
+        return self.model.focused and !self.input_suspended;
