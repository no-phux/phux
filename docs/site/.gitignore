# build output
dist/
.astro/

# deps
node_modules/

# env / local
.env
.env.*
!.env.example
.dev.vars

# os
.DS_Store

# synced docs (regenerated by scripts/sync-docs.ts at build time)
src/content/docs/_synced/
public/docs-manifest.json

# curl installer (copied verbatim from scripts/install.sh by sync-docs.ts).
# Never commit these: the repo script is the single source of truth, and a
# checked-in copy is a second one waiting to go stale.
public/install
public/install.sh

# latest-release stamp (regenerated by scripts/fetch-release.ts at build time)
src/lib/release.json

# wrangler
.wrangler/

# native dev bridge build


# phux-edge: Rust build outputs (the staged artifact in worker/edge/ IS committed)
edge/target/
edge/pkg/
worker/.dev.vars

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
