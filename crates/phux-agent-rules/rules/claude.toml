# Agent-state detection manifest for Claude Code (ADR-0046).
#
# Every predicate here is derived from the shipped CLI's own observable
# output. Provenance for each is recorded on the rule, because a rule whose
# provenance nobody can restate is a rule nobody can safely change.
#
# The governing doctrine (ADR-0046 §D): a MISSED signal is cheap, a FALSE
# `blocked` is not. A permanently-red sidebar destroys trust in the whole
# feature and is strictly worse than having no feature. So: when in doubt,
# do not write the rule. An under-specified manifest degrades to `idle`,
# which is the fail-safe the detector applies when nothing matches.
#
# Claude Code's own internal state is a tri-state (idle / busy / waiting).
# Only `busy` animates the title prefix; `waiting` (a permission dialog is
# up) carries the SAME static prefix as `idle`. So the title proves
# `working` and nothing else — `blocked` must come from the screen. That
# asymmetry is why the title rules below only ever assert `working`.

kind = "claude"
name = "claude"

# `claude` is the argv[0] basename of the native binary. `claude-code` is a
# path component of the npm install (`.../@anthropic-ai/claude-code/cli.js`),
# which runs under a `node` wrapper; see `agent_detect::identify`.
binaries = ["claude", "claude-code"]

# --- Title rules (highest-value, cheapest signal) ---------------------------

# While Claude Code is busy, it writes an ANIMATED prefix glyph into the OSC 0
# title. When it is NOT busy the prefix is the static U+2733 (EIGHT SPOKED
# ASTERISK). The title is therefore a direct, structural, no-substring
# statement by the agent about whether it is working — and it flips the
# instant the turn starts or ends.
#
# THE BUSY GLYPH SET IS VERSION-DRIFTING, so this rule matches a SET, not a
# pair. 2.1.207 alternated U+2802 (BRAILLE DOTS-2) / U+2810 (BRAILLE DOTS-5)
# every ~960 ms. By 2.1.228 the busy prefix is U+25D0 (CIRCLE WITH LEFT HALF
# BLACK) / U+25D1 (CIRCLE WITH RIGHT HALF BLACK) — a different pair entirely,
# so the braille-only regex this rule shipped with matched NOTHING on 2.1.228
# and the highest-priority working signal was silently dead there.
#
# VERIFIED for 2.1.228 against the committed raw-byte capture
# `research/2026-08-12-osc-9-4-claude-code/claude-title-enabled.rawcap`, whose
# twelve OSC 0 titles contain exactly three distinct prefixes: U+2733 (quiet),
# U+25D0 and U+25D1 (busy). No braille appears in that capture. The 2.1.207
# braille pair is KEPT rather than replaced: it was verified against a real
# CLI too, phux does not pin the user's Claude Code version, and a stale
# alternative in a character class costs one branch and no false positives —
# neither glyph occurs at the head of an ordinary window title.
#
# This drift is the reason the two backstops below exist and matter: it was
# caught only because `osc-progress-working` and `screen-status-elapsed-
# backstop` still proved `working` on 2.1.228 while this rule did not. The
# tests for this rule use synthetic title constants, so they were green
# throughout and could not have caught it — only a fresh capture could.
#
# The static U+2733 is deliberately NOT given an `idle` rule: it means "not
# busy", which covers BOTH idle and waiting-on-a-permission-dialog. Asserting
# `idle` from it would outrank (title beats screen) the permission-dialog
# `blocked` rule below and mask every permission prompt. "Not busy" is already
# the detector's fail-safe default, so the absence of a match says it for us.
[[rules]]
id = "title-busy-spinner"
state = "working"
priority = 100
region = "title"
match = { line-regex = "^[⠂⠐◐◑]" }

# Claude Code 2.1.227/228 emits OSC 9;4 independently of OSC 0 titles: `4;3;`
# when a turn starts and `4;0;` when it ends, including when
# CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1. Permission prompts were not captured,
# so these stay below the blocked screen rule's priority.
[[rules]]
id = "osc-progress-working"
state = "working"
priority = 70
region = "osc-progress"
match = { line-regex = '^4;3;?$' }

[[rules]]
id = "osc-progress-idle"
state = "idle"
priority = 70
visible-idle = true
region = "osc-progress"
match = { line-regex = '^4;0;?$' }

# --- Screen rules -----------------------------------------------------------

# A screen-side `working` backstop, for installs that set
# `CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1` and therefore never write the busy
# title above. Priority is lower than the permission-dialog rule so a
# genuinely blocked screen still wins if both ever matched at once (they
# should not: the dialog replaces this status line).
#
# VERIFIED against Claude Code 2.1.207 (captured screen:
# `fixtures/claude/working.txt` line 18):
#
#     ✻ Kneading… (1s · thinking with high effort)
#
# The CLI cycles both the leading glyph and the status verb ("Kneading",
# "Pondering", …) on a rotation that was not fully captured — only this one
# frame was. The rule therefore matches by STRUCTURE, not by the literal
# glyph or verb: any token ending in an ellipsis immediately followed by an
# elapsed-seconds parenthetical, e.g. "…(1s". That shape is not one ordinary
# transcript prose produces, and `bottom-lines` (the same default window the
# transcript-viewer rule below already relies on) keeps it off scrolled-back
# text.
[[rules]]
id = "screen-status-elapsed-backstop"
state = "working"
priority = 60
region = "bottom-lines"
match = { line-regex = '^\S+\s+\S*…\s+\(\d+s' }

# A permission prompt: the question stem plus a numbered option list, with a
# U+276F caret on the selected row.
#
# The question stem is dynamic in the CLI ("Do you want to " + proceed? /
# make this edit to / create / overwrite / write to / insert / delete), so we
# match the stable prefix rather than each leaf.
#
# VERIFIED against Claude Code 2.1.207 (captured screen:
# `tests/fixtures/claude/blocked_permission.txt`). The dialog REPLACES the
# input box and is the only thing below the final rule:
#
#     ────────────────────────────────────────
#      Bash command
#        touch /tmp/probe.txt
#      Do you want to proceed?
#      ❯ 1. Yes
#        2. Yes, and allow access to tmp/
#        3. No
#      Esc to cancel · Tab to amend · ctrl+e to explain
#
# `after-last-rule` is what makes this safe: the transcript — where Claude
# might legitimately PRINT the words "do you want to proceed?" inside a diff
# or a quoted shell session — always sits ABOVE the rules, so it is
# structurally outside the region and cannot be seen. Requiring a numbered
# option line as well is a second, independent guard.
#
# The question stem is dynamic in the CLI ("Do you want to " + proceed? /
# make this edit to / create / overwrite / …), so we match the stable prefix.
[[rules]]
id = "prompt-permission-dialog"
state = "blocked"
priority = 80
region = "after-last-rule"
match = { all = [
  { any = [
    { contains = "do you want to " },
    { contains = "would you like to proceed" },
  ] },
  { line-regex = '^\s*(❯\s*)?\d+\.\s+\S' },
] }

# --- Freeze rules -----------------------------------------------------------

# The transcript viewer (ctrl+o) is a pager over scrolled-back history. It
# hides the live chrome entirely, so the screen carries NO information about
# what the agent is doing. Freeze the last derivation rather than guess.
#
# VERIFIED against 2.1.207: the viewer's footer reads
#   "Showing detailed transcript · ctrl+o to toggle · ↑↓ scroll · …"
#
# This also covers the dialog-pending-behind-the-viewer case without needing
# a rule of its own: a permission dialog is on screen BEFORE the user can
# press ctrl+o, so `blocked` is already the published state by the time the
# viewer opens, and freezing preserves it.
[[rules]]
id = "transcript-viewer"
priority = 200
region = "bottom-lines"
skip-state-update = true
match = { contains = "showing detailed transcript" }

# --- Rules deliberately NOT written ------------------------------------------
#
# Earlier drafts carried three more rules, each keyed on a string that does
# not exist in the shipped CLI: a "dialog waiting" blocked badge, a
# "<chord> to interrupt" working hint, and a "? for shortcuts" idle hint.
# All three were removed after being checked against the real TUI.
#
# The last is the instructive one. "? for shortcuts" DOES appear in 2.1.207 —
# but only in the transcript viewer's footer, never beneath the idle input
# box. As an `idle` + `visible-idle` rule it would therefore have asserted
# positive idle evidence in the one state that carries no information at all,
# and `visible-idle` is precisely what bypasses the working -> idle debounce.
# A plausible-sounding string is not a verified one.
#
# Their absence costs nothing: `idle` is the detector's fail-safe default
# (ADR-0046 §D), so it needs no rule to be reached, and `working` is proven
# by the title above. The screen-side `working` backstop this section used to
# say was missing (for `CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1` installs) is
# now `screen-status-elapsed-backstop` above, once the elapsed-status line
# was captured.
#
# Still not written: a positive `idle` rule DERIVED FROM THE SCREEN OR THE
# TITLE. That is what phux-w7z2.28 ruled against, and it stays ruled against
# for the reasons above — live claude chrome is byte-identical idle vs
# working, and the quiet U+2733 title means "not busy", which covers a
# permission dialog too.
#
# `osc-progress-idle` above is NOT a counter-example to that ruling, and the
# distinction is the whole reason it was allowed to ship. It reads neither the
# screen nor the title: it reads OSC 9;4, a channel on which the CLI makes an
# EXPLICIT, POSITIVE statement that the turn's progress indicator was removed.
# The two objections to a screen idle rule both fail against it — there is no
# ambiguity with a permission dialog (a dialog does not clear progress), and
# it cannot fire alongside its own `working` twin, because the `osc-progress`
# region carries exactly one latest payload, so `4;3;` and `4;0;` are mutually
# exclusive within a single evaluation. That last property is what makes its
# `visible-idle = true` safe: the working -> idle hold exists to debounce the
# AMBIGUOUS case (the screen stopped saying "working" without saying "idle"),
# and here the agent said "idle" outright.
#
# Also still not written: any of the 16 kinds phux-w7z2.21 lists with no
# manifest at all, plus gemini (phux-7p8). No CLI for any of them is installed
# or authenticated in this environment, so none has a captured screen to
# verify a rule against. A rule matching a screen nobody has captured is a
# guess, and ADR-0046's Tradeoffs already record what that costs.
